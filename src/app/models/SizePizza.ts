export interface SizePizza {
    sizePizzaId  ?: number,
    deliveryCost ?: number;
    euroPrice ?: number;
    pizzaId  ?: string,
    sizeId  ?: number,
    price  ?: number,
    creationDate  ?: Date,
    status  ?: string;
    name ?: string;
}
