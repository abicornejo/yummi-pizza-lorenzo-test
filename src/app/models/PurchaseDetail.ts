export interface PurchaseDetails {
    sizePizzaId  ?: string,
    pizzaId  ?: string,
    sizeId  ?: string,
    price  ?: number,
    creationDate  ?: Date,
    status  ?: string;
    name ?: string;
}
