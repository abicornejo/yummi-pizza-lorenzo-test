export interface Pizza {
    pizzaId ?: number;
    name ?: string;
    description ?: string;
    image ?: string;
    price ?: string;
    stock ?: string;
    minimum_stock ?: string;
    creationDate ?: Date;
    status ?: string;
}
